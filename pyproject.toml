[build-system]
requires = ["setuptools>=68.0", "setuptools-scm"]
build-backend = "setuptools.build_meta"

[project]
name = "autocue"
version = "0.1.0"
description = "Auto-cue hot cue placement for Engine DJ libraries"
requires-python = ">=3.11"
dependencies = [
    "pyyaml>=6.0",
]

[project.optional-dependencies]
analysis = [
    "librosa>=0.10",
    "numpy>=1.24",
    "scipy>=1.11",
    "pyyaml>=6.0",
]
gui = [
    "flask>=3.0",
    "mutagen>=1.47",
]
beats = [
    "beat-this",
    "torch>=2.0",
    "torchaudio>=2.0",
    "numpy>=1.24",
    "soundfile>=0.12",
]
dev = [
    "pytest>=7.0",
    "pytest-cov",
]

[project.scripts]
autocue = "autocue.cli:main"

[tool.setuptools.packages.find]
where = ["src"]

[tool.setuptools.package-data]
"autocue.templates" = ["*.yaml"]
"autocue" = ["static/*.html", "static/vendor/*.js"]

[tool.pytest.ini_options]
testpaths = ["tests"]
